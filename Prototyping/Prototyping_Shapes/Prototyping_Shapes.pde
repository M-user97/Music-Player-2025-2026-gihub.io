//size(); //width //height
fullScreen(); //displayWidth //displayHeight
int appWidth = displayWidth;
int appHeight = displayHeight;
//rect(x, y, width, height);
//Note: the debuggger expects rectangles to have float or double type variables
//Using Ratios
float exitX = appWidth * 0.2/22.5;
float exitY = appHeight * 0.2/12.5;
float exitWidth = appWidth * 1/22.5;
float exitHeight = appHeight * 1/12.5;

rect(exitX, exitY, exitWidth, exitHeight);
//
float homeX = appWidth * 21.3/22.5;
float homeY = appHeight * 0.2/12.5;
float homeWidth = appWidth * 1/22.5;
float homeHeight = appHeight * 1/12.5;

rect(homeX, homeY, homeWidth, homeHeight);
//
float imageX = appWidth * 7.5/22.5;
float imageY = appHeight * 0.7/12.5;
float imageWidth = appWidth * 7.5/22.5;
float imageHeight = appHeight * 5.8/12.5;

rect(imageX, imageY, imageWidth, imageHeight);
//
float playX = appWidth * 10.4/22.5;
float playY = appHeight * 9.2/12.5;
float playWidth = appWidth * 1.8/22.5;
float playHeight = appHeight * 1.9/12.5;

rect(playX, playY, playWidth, playHeight);
//
float skipleftX = appWidth * 8.4/22.5;
float skipleftY = appHeight * 9.2/12.5;
float skipleftWidth = appWidth * 1.8/22.5;
float skipleftHeight = appHeight * 1.9/12.5;

rect(skipleftX, skipleftY, skipleftWidth, skipleftHeight);
//
float skiprightX = appWidth * 12.4/22.5;
float skiprightY = appHeight * 9.2/12.5;
float skiprightWidth = appWidth * 1.8/22.5;
float skiprightHeight = appHeight * 1.9/12.5;

rect(skiprightX, skiprightY, skiprightWidth, skiprightHeight);
//
float repeatX = appWidth * 14.4/22.5;
float repeatY = appHeight * 9.4/12.5;
float repeatWidth = appWidth * 1.4/22.5;
float repeatHeight = appHeight * 1.4/12.5;

rect(repeatX, repeatY, repeatWidth, repeatHeight);
//
float muteX = appWidth * 7.2/22.5;
float muteY = appHeight * 10.2/12.5;
float muteWidth = appWidth * 01/22.5;
float muteHeight = appHeight * 1/12.5;

rect(muteX, muteY, muteWidth, muteHeight);
//
float stopX = appWidth * 7.2/22.5;
float stopY = appHeight * 9.1/12.5;
float stopWidth = appWidth * 1/22.5;
float stopHeight = appHeight * 1/12.5;

rect(stopX, stopY, stopWidth, stopHeight);
//
float timescrollX = appWidth * 6.9/22.5;
float timescrollY = appHeight * 8.3/12.5;
float timescrollWidth = appWidth * 8.9/22.5;
float timescrollHeight = appHeight * 0.5/12.5;

rect(timescrollX, timescrollY, timescrollWidth, timescrollHeight);
//
float volumeswtichX = appWidth * 0.6/22.5;
float volumeswtichY = appHeight * 3.4/12.5;
float volumeswtichWidth = appWidth * 0.7/22.5;
float volumeswtichHeight = appHeight * 4/12.5;

rect(volumeswtichX, volumeswtichY, volumeswtichWidth, volumeswtichHeight);
//
float volumeiconX = appWidth * 0.3/22.5;
float volumeiconY = appHeight * 7.7/12.5;
float volumeiconWidth = appWidth * 1.3/22.5;
float volumeiconHeight = appHeight * 1.4/12.5;

rect(volumeiconX, volumeiconY, volumeiconWidth, volumeiconHeight);
//
float moredot1X = appWidth * 21.7/22.5;
float moredot1Y = appHeight * 9.17/12.5;
float moredot1Width = appWidth * 0.5/22.5;
float moredot1Height = appHeight * 0.5/12.5;

rect(moredot1X, moredot1Y, moredot1Width, moredot1Height);
//
float moredot2X = appWidth * 21.7/22.5;
float moredot2Y = appHeight * 9.8/12.5;
float moredot2Width = appWidth * 0.5/22.5;
float moredot2Height = appHeight * 0.5/12.5;

rect(moredot2X, moredot2Y, moredot2Width, moredot2Height);
//
float moredot3X = appWidth * 21.7/22.5;
float moredot3Y = appHeight * 10.43/12.5;
float moredot3Width = appWidth * 0.5/22.5;
float moredot3Height = appHeight * 0.5/12.5;
//
rect(moredot3X, moredot3Y, moredot3Width, moredot3Height);
//
float shareX = appWidth * 20.1/22.5;
float shareY = appHeight * 9.3/12.5;
float shareWidth = appWidth * 1.4/22.5;
float shareHeight = appHeight * 1.5/12.5;

rect(shareX, shareY, shareWidth, shareHeight);
//
float playlistX = appWidth * 18.5/22.5;
float playlistY = appHeight * 9.3/12.5;
float playlistWidth = appWidth * 1.4/22.5;
float playlistHeight = appHeight * 1.5/12.5;

rect(playlistX, playlistY, playlistWidth, playlistHeight);
//
float songtimeX = appWidth * 5.5/22.5;
float songtimeY = appHeight * 8.25/12.5;
float songtimeWidth = appWidth * 1.3/22.5;
float songtimeHeight = appHeight * 0.6/12.5;

rect(songtimeX, songtimeY, songtimeWidth, songtimeHeight);
//
float songendX = appWidth * 15.9/22.5;
float songendY = appHeight * 8.25/12.5;
float songendWidth = appWidth * 1.3/22.5;
float songendHeight = appHeight * 0.6/12.5;

rect(songendX, songendY, songendWidth, songendHeight);
//
float titleX = appWidth * 6.8/22.5;
float titleY = appHeight * 6.9/12.5;
float titleWidth = appWidth * 4.5/22.5;
float titleHeight = appHeight * 1/12.5;

rect(titleX, titleY, titleWidth, titleHeight);
//
float albumartistX = appWidth * 11.3/22.5;
float albumartistY = appHeight * 6.9/12.5;
float albumartistWidth = appWidth * 4.5/22.5;
float albumartistHeight = appHeight * 1/12.5;

rect(albumartistX, albumartistY, albumartistWidth, albumartistHeight);
//
float skipPaddingX = skiprightWidth * 0.2;
float skipPaddingY = skiprightHeight * 0.2;

float nextTriX1 = skiprightX + skipPaddingX;
float nextTriY1 = skiprightY + skipPaddingY;
float nextTriX2 = skiprightX + skipPaddingX;
float nextTriY2 = skiprightY + skiprightHeight - skipPaddingY;
float nextTriX3 = skiprightX + skiprightWidth - skipPaddingX;
float nextTriY3 = skiprightY + skiprightHeight / 2;

triangle(nextTriX1, nextTriY1, nextTriX2, nextTriY2, nextTriX3, nextTriY3);

float barWidth = skiprightWidth * 0.12;
float barX = skiprightX + skiprightWidth - skipPaddingX - barWidth;
float barY = skiprightY + skipPaddingY;
float barHeight = skiprightHeight - 2 * skipPaddingY;
rect(barX, barY, barWidth, barHeight);
//
float skipLeftPaddingX = skipleftWidth * 0.2;
float skipLeftPaddingY = skipleftHeight * 0.2;

float prevTriX1 = skipleftX + skipleftWidth - skipLeftPaddingX;
float prevTriY1 = skipleftY + skipLeftPaddingY;
float prevTriX2 = skipleftX + skipleftWidth - skipLeftPaddingX;
float prevTriY2 = skipleftY + skipleftHeight - skipLeftPaddingY;
float prevTriX3 = skipleftX + skipLeftPaddingX;
float prevTriY3 = skipleftY + skipleftHeight / 2;

triangle(prevTriX1, prevTriY1, prevTriX2, prevTriY2, prevTriX3, prevTriY3);

float prevBarWidth = skipleftWidth * 0.12;
float prevBarX = skipleftX + skipLeftPaddingX;
float prevBarY = skipleftY + skipLeftPaddingY;
float prevBarHeight = skipleftHeight - 2 * skipLeftPaddingY;
rect(prevBarX, prevBarY, prevBarWidth, prevBarHeight);
//
float plusCenterX = playlistX + playlistWidth / 2;
float plusCenterY = playlistY + playlistHeight / 2;
float plusHalfLength = min(playlistWidth, playlistHeight) * 0.27;
//
line(plusCenterX, plusCenterY - plusHalfLength, plusCenterX, plusCenterY + plusHalfLength);
//
line(plusCenterX - plusHalfLength, plusCenterY, plusCenterX + plusHalfLength, plusCenterY);
//
float houseCenterX = homeX + homeWidth / 2;
float houseCenterY = homeY + homeHeight / 2;
float houseWidth = homeWidth * 0.55;
float houseHeight = homeHeight * 0.38;
//
float baseX = houseCenterX - houseWidth / 2;
float baseY = houseCenterY;
rect(baseX, baseY, houseWidth, houseHeight);

//
float roofCenterY = houseCenterY;
triangle(
  houseCenterX - houseWidth / 2, roofCenterY,
  houseCenterX + houseWidth / 2, roofCenterY,
  houseCenterX, roofCenterY - houseHeight * 0.8
);

float doorWidth = houseWidth * 0.22;
float doorHeight = houseHeight * 0.50;
float doorX = houseCenterX - doorWidth / 2;
float doorY = baseY + houseHeight - doorHeight;
rect(doorX, doorY, doorWidth, doorHeight);
//
float xCenterX = exitX + exitWidth / 2;
float xCenterY = exitY + exitHeight / 2;
float xHalfLength = min(exitWidth, exitHeight) * 0.34;


line(xCenterX - xHalfLength, xCenterY - xHalfLength, xCenterX + xHalfLength, xCenterY + xHalfLength);

line(xCenterX - xHalfLength, xCenterY + xHalfLength, xCenterX + xHalfLength, xCenterY - xHalfLength);
//
float boxWidth = shareWidth * 0.60;
float boxHeight = shareHeight * 0.31;
float boxLeft = shareX + (shareWidth - boxWidth) / 2;
float boxTop = shareY + shareHeight * 0.51;
float boxRight = boxLeft + boxWidth;
float boxBottom = boxTop + boxHeight;

stroke(0);
noFill();
beginShape();
vertex(boxLeft, boxTop);
vertex(boxLeft, boxBottom);
vertex(boxRight, boxBottom);
vertex(boxRight, boxTop);
endShape();

float arrowBaseX = shareX + shareWidth / 2;
float arrowBaseY = boxTop;
float arrowTipY = shareY + shareHeight * 0.32;
float arrowHeadSize = shareWidth * 0.17;

line(arrowBaseX, arrowBaseY, arrowBaseX, arrowTipY);

triangle(
  arrowBaseX, arrowTipY - arrowHeadSize * 0.5,
  arrowBaseX - arrowHeadSize * 0.50, arrowTipY + arrowHeadSize * 0.35,
  arrowBaseX + arrowHeadSize * 0.50, arrowTipY + arrowHeadSize * 0.35
);

noFill();
stroke(0);
//
float squareDotSize = min(moredot1Width, moredot1Height) * 0.7;
float squareDotX = moredot1X + (moredot1Width - squareDotSize) / 2;
float squareDotY = moredot1Y + (moredot1Height - squareDotSize) / 2;

fill(40);
noStroke();
rect(squareDotX, squareDotY, squareDotSize, squareDotSize);

noFill();
stroke(0);
//
float squareDotSize2 = min(moredot2Width, moredot2Height) * 0.7;
float squareDotX2 = moredot2X + (moredot2Width - squareDotSize2) / 2;
float squareDotY2 = moredot2Y + (moredot2Height - squareDotSize2) / 2;

fill(40);
noStroke();
rect(squareDotX2, squareDotY2, squareDotSize2, squareDotSize2);

noFill();
stroke(0);
//
float squareDotSize3 = min(moredot3Width, moredot3Height) * 0.7;
float squareDotX3 = moredot3X + (moredot3Width - squareDotSize3) / 2;
float squareDotY3 = moredot3Y + (moredot3Height - squareDotSize3) / 2;

fill(40);
noStroke();
rect(squareDotX3, squareDotY3, squareDotSize3, squareDotSize3);

noFill();
stroke(0);
//
float stopCenterX = stopX + stopWidth / 2;
float stopCenterY = stopY + stopHeight / 2;
float stopSquareSize = min(stopWidth, stopHeight) * 0.56;

float stopSquareX = stopCenterX - stopSquareSize / 2;
float stopSquareY = stopCenterY - stopSquareSize / 2;
rect(stopSquareX, stopSquareY, stopSquareSize, stopSquareSize);
//
String albumTitle = "Blue Moon";
textAlign(CENTER, CENTER);
textSize(min(titleX, titleWidth, titleHeight) * 0.5);
fill(0);
float titleCenterX = titleX + titleWidth / 2;
float titleCenterY = titleY + titleHeight / 2;

text(albumTitle, titleCenterX, titleCenterY);

textAlign(LEFT, BASELINE);
textSize(12);
noFill();
//
String albumArtist = "JVNA";
textAlign(CENTER, CENTER);
textSize(min(albumartistWidth, albumartistHeight) * 0.5);
fill(0);
float artistCenterX = albumartistX + albumartistWidth / 2;
float artistCenterY = albumartistY + albumartistHeight / 2;

text(albumArtist, artistCenterX, artistCenterY);

textAlign(LEFT, BASELINE);
textSize(12);
noFill();
//
//
float boxPadding = min(muteWidth, muteHeight) * 0.18;
float boxSize = min(muteWidth, muteHeight) - 2 * boxPadding;
float boxX = muteX + (muteWidth - boxSize) / 2;
float boxY = muteY + (muteHeight - boxSize) / 2;

//
noFill();
stroke(0);
strokeWeight(1);
rect(boxX, boxY, boxSize, boxSize);

//
float crossPadding = boxSize * 0.23;
float x1 = boxX + crossPadding;
float y1 = boxY + crossPadding;
float x2 = boxX + boxSize - crossPadding;
float y2 = boxY + boxSize - crossPadding;
line(x1, y1, x2, y2);
line(x1, y2, x2, y1);
strokeWeight(1);
//
//
float paddingX = playWidth * 0.2;
float paddingY = playHeight * 0.2;

float triX1 = playX + paddingX;
float triY1 = playY + paddingY;
float triX2 = playX + paddingX;
float triY2 = playY + playHeight - paddingY;
float triX3 = playX + playWidth - paddingX;
float triY3 = playY + playHeight / 2;

triangle(triX1, triY1, triX2, triY2, triX3, triY3);

// already in your code
//square(nameX, nameY, nameDimension);
//line(nameX1, nameY1, nameX2, nameY2);
//triangle(nameX1, nameY1, nameX2, nameY2, nameX3, nameY3);
